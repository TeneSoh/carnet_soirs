from django.db import models
# from django.contrib.auth.models import abstractUser, BaseUserManager

# Create your models here.
# class (Usermanager):
#     user_in_migation = True
#     def create_user(self, email, passwoed=None, **astra_fields):
#         if not email
#             raise ValueError("the email field is requires")
#         email = self.normlize_email(email=email)
    